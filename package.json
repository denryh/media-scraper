{
  "name": "media-scraper",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter frontend lint",
    "test:integration": "bun run --filter backend test:integration"
  }
}
